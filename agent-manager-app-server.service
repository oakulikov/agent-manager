[Unit]
Description=Codex App Server for Funxy Agent Manager
After=default.target

[Service]
Type=simple
ExecStart=/bin/bash -lc "exec codex app-server --listen ws://127.0.0.1:14551"
Restart=on-failure
RestartSec=2

[Install]
WantedBy=default.target
